# Build artifacts

Este directorio se reserva para estado local de build y cachés. Los artefactos generados no se versionan. Las herramientas deben crear subdirectorios explícitos y nunca usar rutas temporales ambiguas como destino de una imagen virtual.
