# Imágenes virtuales

Solo se almacenarán aquí imágenes OCI exportadas, discos `qcow2`, snapshots y logs de la VM. Este directorio nunca puede apuntar a una letra de unidad, partición o dispositivo físico del host.
