# Defaults used by the nested Podman invoked by Image Builder for Phase 0.
# Force the inner bootc inspection/build containers to have no network, so the
# nested runtime does not create a bridge or require netavark.
[containers]
netns = "none"
no_hosts = true
